{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "!(**/*.js|**/*.css|**/*.png|**/*.json)",
        "destination": "/index.html"
      }
    ]
  }
}
